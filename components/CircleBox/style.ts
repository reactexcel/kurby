import { KBColor } from "constants/color";

export const useStyles = {
    boxCard: {
        width: '32px',
        height: '32px',
        borderRadius: '50%',
        backgroundColor: KBColor.GREY,
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
    }
}